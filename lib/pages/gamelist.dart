class GameList {
  String id;
  String game_title;
  String catogeries;
  String img_url;
  bool isSeleted;

  GameList(
      this.id, this.game_title, this.catogeries, this.img_url, this.isSeleted);
}
